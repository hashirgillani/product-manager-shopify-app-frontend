# Frontend configuration (product-manager-frontend)
# Public URL of the backend API, e.g. https://api.your-domain.com
# Leave empty during local dev so Vite proxies /api to the backend.
VITE_API_URL=

# Injected into index.html (the shopify-api-key meta tag) at build time.
SHOPIFY_API_KEY=